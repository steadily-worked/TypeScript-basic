// function sum(a, b) {
//   return a + b;
// }
// sum(10, "20"); // JS 특성 상 문자 + 숫자는 전부 문자열로 취급함. 따라서 1020이 출력

// sum()

/**
 *
 * @param {number} a
 * @param {number} b
 */

function sum(a, b) {
  return a + b;
}

sum(10, 20);
